# JVM arguments for large-scale testing
org.gradle.jvmargs=-Xmx8g -Xms2g -XX:MaxMetaspaceSize=512m -XX:+UseG1GC -XX:G1HeapRegionSize=32m

# Enable parallel compilation
org.gradle.parallel=true
org.gradle.configureondemand=true

# Large-scale data testing optimizations
org.gradle.daemon=true